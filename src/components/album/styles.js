import styled from "styled-components";

const Container = styled.div`
  img {
    max-width: 100%;
    display: block;
  }

  p {
    text-align: center;
    color: #fff;
  }
`;

export default Container;
